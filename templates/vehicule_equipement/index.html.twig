{% extends 'base.html.twig' %}

{% block title %}VehiculeEquipement index{% endblock %}

{% block body %}
    <h1>VehiculeEquipement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>NomLong</th>
                <th>Poids</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for vehicule_equipement in vehicule_equipements %}
            <tr>
                <td>{{ vehicule_equipement.nomLong }}</td>
                <td>{{ vehicule_equipement.poids }}</td>
                <td>
                    <a href="{{ path('vehicule_equipement_show', {'vehicule': vehicule_equipement.vehicule}) }}">show</a>
                    <a href="{{ path('vehicule_equipement_edit', {'vehicule': vehicule_equipement.vehicule}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('vehicule_equipement_new') }}">Create new</a>
{% endblock %}
