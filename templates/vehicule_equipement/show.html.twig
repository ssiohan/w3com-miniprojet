{% extends 'base.html.twig' %}

{% block title %}VehiculeEquipement{% endblock %}

{% block body %}
    <h1>VehiculeEquipement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NomLong</th>
                <td>{{ vehicule_equipement.nomLong }}</td>
            </tr>
            <tr>
                <th>Poids</th>
                <td>{{ vehicule_equipement.poids }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vehicule_equipement_index') }}">back to list</a>

    <a href="{{ path('vehicule_equipement_edit', {'vehicule': vehicule_equipement.vehicule}) }}">edit</a>

    {{ include('vehicule_equipement/_delete_form.html.twig') }}
{% endblock %}
